import { SettingsManager } from "@/components/admin/settings-manager";

export const metadata = { title: "Delivery & Order Settings — Admin" };

export default function AdminSettingsPage() {
  return <SettingsManager />;
}
