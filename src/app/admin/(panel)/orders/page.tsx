import { OrdersManager } from "@/components/admin/orders-manager";

export const metadata = { title: "Order Enquiries — Admin" };

export default function AdminOrdersPage() {
  return <OrdersManager />;
}
