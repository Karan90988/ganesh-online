import { ImportManager } from "@/components/admin/import-manager";

export const metadata = { title: "Excel Import — Admin" };

export default function AdminImportPage() {
  return <ImportManager />;
}
