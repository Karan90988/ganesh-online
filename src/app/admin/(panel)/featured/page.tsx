import { FeaturedManager } from "@/components/admin/featured-manager";

export const metadata = { title: "Home Page Products — Admin" };

export default function AdminFeaturedPage() {
  return <FeaturedManager />;
}
